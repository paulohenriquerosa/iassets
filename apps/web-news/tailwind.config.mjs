const config = {
  content: ['./src/**/*.{js,ts,jsx,tsx}'],
  darkMode: false, // Desativa completamente dark mode e variantes
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config; 